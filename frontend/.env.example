# Copy to .env for local overrides, or set as a Vercel Environment Variable in production.
# Leave unset locally to use the Vite dev proxy (see vite.config.js).
VITE_API_URL=https://quiz-management-and-online-assessment-j3ez.onrender.com/api
